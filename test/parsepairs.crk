fun parsepairs(xs : Int*) : (Int . Int)* =
    case xs of
      nil => nil
    | y :: ys => case ys of
                   nil => nil
                 | z :: zs => (y;z) :: rec(zs)

exec step parsepairs([2))
exec parsepairs((emp;[3)))