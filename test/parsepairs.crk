fun parsepairs(xs : Int*) : (Int . Int)* =
    case xs of
      nil => nil
    | y :: ys => case ys of
                   nil => nil
                 | z :: zs => (y;z) :: rec(zs)

exec step parsepairs(xs = [2))
exec parsepairs(xs = (emp;[3)))